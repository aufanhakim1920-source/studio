Live: https://aufanhakim1920-source.github.io/studio/

Studio site at the root, every build under /work/.
Hand-built HTML, CSS and vanilla JS. No framework, no build step.
All work is self-initiated or spec.

## Deploy flow (run in this order)

1. \
2. add the row to \
3. \
4. **\stripped 2 files — 11 lines, 0.8 kB removed** — removes internal design commentary from the PUBLISHED
   copy only. This repo is public and staying public; Aufan's rule is that people may have
   the final product, not the process. Source in the learning folder and the vault keeps
   every comment. Conservative by design: for .js it strips only the banner at the top of
   the file, because GLSL lives inside template literals and a naive regex mangles shaders.
5. \index.html + work/index.html generated — 43 builds, 6 favourites
types: Portfolios 12, Landing pages 20, Storefronts 6, Dashboards 3, Product UI 2, Games 0, Editorial 0, Courses 0
6. commit as \ — **never any Claude attribution**
